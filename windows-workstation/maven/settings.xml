<?xml version="1.0" encoding="UTF-8"?>

<!--
  USER LEVEL configuration file for Maven.
    is normally provided in ${user.home}/.m2/settings.xml.
    NOTE: This location can be overridden with the CLI option:
      -s /path/to/user/settings.xml
 -->
<settings xmlns="http://maven.apache.org/SETTINGS/1.0.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/SETTINGS/1.0.0 http://maven.apache.org/xsd/settings-1.0.0.xsd">
  <!-- localRepository
       The path to the local repository maven will use to store artifacts.
       Default: ${user.home}/.m2/repository -->
  <localRepository>c:/stegdav/maven-data/repository</localRepository>
  
  <proxies>
    <!-- Specification for one proxy, to be used in connecting to the network.-->
    <proxy>
      <id>deutsche-boerse</id>
      <active>true</active>
      <protocol>http</protocol>
      <username></username>
      <password></password>
      <host>proxy.example.com</host>
      <port>8080</port>
      <nonProxyHosts>*.example.com|*.intranet.local</nonProxyHosts>
    </proxy>
  </proxies>

</settings>
